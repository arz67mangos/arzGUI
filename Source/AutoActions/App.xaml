<Application x:Class="AutoActions.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutoActions"
             xmlns:views="clr-namespace:AutoActions.Views"
             xmlns:uwp="clr-namespace:AutoActions.UWP"
             xmlns:prflActns="clr-namespace:AutoActions.Profiles.Actions"
             xmlns:info="clr-namespace:AutoActions.Info"
             xmlns:thm="clr-namespace:AutoActions.Theming"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:pres="clr-namespace:AutoActions.ProjectResources;assembly=AutoActions.ProjectResources"
             xmlns:corewpf="clr-namespace:CodectoryCore.UI.Wpf;assembly=ArzGUI.Foundation">
    <!--StartupUri="Views\AutoActionsMainView.xaml">-->

    <Application.Resources>
        <ResourceDictionary>

            <!-- AppResources.xaml is GENERATED from Controls/1_Colors, 2_DefaultControls, 3_CustomComboBox.
                 ThemeManager appends Theming/0_LightColors.xaml or 0_DarkColors.xaml after it at runtime. -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Controls\AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <system:String x:Key="UpstreamRepoLink">https://github.com/Codectory/AutoActions</system:String>
            <system:String x:Key="ForkRepoLink">https://github.com/arz67mangos/arz-AutoActions-edit</system:String>

            <!-- Tabs default to the sidebar look; dialogs that still use a plain TabControl get it too. -->
            <Style x:Key="DefaultTabHeader" TargetType="TabItem" BasedOn="{StaticResource SidebarTabItem}"/>
            <Style TargetType="TabItem" BasedOn="{StaticResource SidebarTabItem}"/>
            <Style TargetType="TabControl" BasedOn="{StaticResource SidebarTabControl}"/>

            <Style x:Key="GridHeaderRight" TargetType="{x:Type GridViewColumnHeader}">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="{StaticResource CaptionFontSize}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                            <Border BorderBrush="{DynamicResource StrokeBrush}" BorderThickness="0,0,0,1" Background="Transparent">
                                <TextBlock Text="{TemplateBinding Content}" Padding="8,6" Width="{TemplateBinding Width}" TextAlignment="Left" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" Typography.Capitals="AllSmallCaps"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridHeaderRight}"/>

            <Style x:Key="DefaultListViewItem" TargetType="{x:Type ListViewItem}">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                            <Border x:Name="ListBoxItemRoot" BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}" CornerRadius="{StaticResource SmallCornerRadius}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                <GridViewRowPresenter Columns="{TemplateBinding GridView.ColumnCollection}" Content="{TemplateBinding Content}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource DefaultListViewItem}"/>

            <!-- Thin overlay-style scrollbars -->
            <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Grid x:Name="Grid">
                                <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="Transparent"/>
                                <Border x:Name="Rectangle1" CornerRadius="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{TemplateBinding Background}"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Tag" Value="Vertical">
                                    <Setter TargetName="Rectangle1" Property="Width" Value="6"/>
                                    <Setter TargetName="Rectangle1" Property="Height" Value="Auto"/>
                                </Trigger>
                                <Trigger Property="Tag" Value="Horizontal">
                                    <Setter TargetName="Rectangle1" Property="Width" Value="Auto"/>
                                    <Setter TargetName="Rectangle1" Property="Height" Value="6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
                <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
                <Setter Property="Foreground" Value="{DynamicResource ScrollThumbBrush}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="GridRoot" Width="10" Background="Transparent">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0.00001*"/>
                                </Grid.RowDefinitions>
                                <Track x:Name="PART_Track" Grid.Row="0" IsDirectionReversed="True" Focusable="False">
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" Style="{DynamicResource ScrollThumbs}"/>
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                    </Track.DecreaseRepeatButton>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger SourceName="Thumb" Property="IsMouseOver" Value="True">
                                    <Setter Value="{DynamicResource ScrollThumbHoverBrush}" TargetName="Thumb" Property="Background"/>
                                </Trigger>
                                <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                                    <Setter Value="{DynamicResource ScrollThumbActiveBrush}" TargetName="Thumb" Property="Background"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="Orientation" Value="Horizontal">
                                    <Setter TargetName="GridRoot" Property="LayoutTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="-90"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="PART_Track" Property="LayoutTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="-90"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Width" Value="Auto"/>
                                    <Setter Property="Height" Value="10"/>
                                    <Setter TargetName="Thumb" Property="Tag" Value="Horizontal"/>
                                    <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                                    <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand"/>
                                </Trigger>
                                <Trigger Property="Orientation" Value="Vertical">
                                    <Setter Property="Width" Value="10"/>
                                    <Setter Property="Height" Value="Auto"/>
                                    <Setter TargetName="Thumb" Property="Tag" Value="Vertical"/>
                                    <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                                    <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ItemsPanelTemplate x:Key="StackPanelTemplate">
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
            <ItemsPanelTemplate x:Key="WrapPanelTemplate">
                <WrapPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
            <ItemsPanelTemplate x:Key="StackVerticalPanelTemplate">
                <StackPanel Orientation="Vertical"/>
            </ItemsPanelTemplate>
            <ItemsPanelTemplate x:Key="WrapVerticalPanelTemplate">
                <WrapPanel Orientation="Vertical"/>
            </ItemsPanelTemplate>

            <!-- The green "+" that adds an action to a profile list -->
            <Style x:Key="AddActionButton" TargetType="Button" BasedOn="{StaticResource DefaultButton}">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Width" Value="32"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Content" Value="+"/>
            </Style>

            <corewpf:EnumLocaleConverter x:Key="EnumLocaleConverter"/>
            <corewpf:DialogService x:Key="DialogService"/>
            <corewpf:DialogService x:Key="ProfileDialogService"/>
            <corewpf:BitmapToBitmapImageConverter x:Key="BitmapToBitmapImageConverter"/>
            <corewpf:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
            <corewpf:MainWindowBase x:Key="MainWindowBase"/>
            <corewpf:IsNotNullConverter x:Key="IsNotNullConverter"/>
            <corewpf:SizeTextConverter x:Key="SizeTextConverter"/>
            <corewpf:IsNotZeroConverter x:Key="IsNotZeroConverter"/>
            <corewpf:StringFormatConcatenator x:Key="StringFormatConcatenator"/>
            <corewpf:VisibilityBooleanConverter x:Key="VisibilityBooleanConverter"/>
            <corewpf:ConverterChain x:Key="InvertVisibilityBooleanConverter">
                <corewpf:InvertBooleanConverter/>
                <corewpf:VisibilityBooleanConverter/>
            </corewpf:ConverterChain>
            <corewpf:ConverterChain x:Key="NotZeroVisibilityBooleanConverter">
                <corewpf:IsNotZeroConverter/>
                <corewpf:VisibilityBooleanConverter/>
            </corewpf:ConverterChain>
            <corewpf:ConverterChain x:Key="NotNullVisibilityConverter">
                <corewpf:IsNotNullConverter/>
                <corewpf:VisibilityBooleanConverter/>
            </corewpf:ConverterChain>
            <corewpf:TupleConverter x:Key="TupleConverter"/>

            <DataTemplate DataType="{x:Type local:ApplicationAdder}">
                <views:ApplicationAdderView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type prflActns:ProfileActionAdder}">
                <views:ProfileActionAdderView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type uwp:UWPApplicationDialog}">
                <views:UWPApplicationDialogView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type info:AutoActionsInfo}">
                <views:AutoActionsInfoView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type info:AutoActionsLicense}">
                <views:AutoActionsLicenseView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type info:LogsStorage}">
                <views:AutoActionsLogsView/>
            </DataTemplate>
            <DataTemplate x:Key="ProfileItemTemplate" DataType="Profile">
                <TextBlock Style="{StaticResource DefaultTextBlock}" Text="{Binding Name}"/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>

</Application>
