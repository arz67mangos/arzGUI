<UserControl x:Class="AutoActions.Views.AutoActionsInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pres="clr-namespace:AutoActions.ProjectResources;assembly=AutoActions.ProjectResources"
             xmlns:info="clr-namespace:AutoActions.Info"
             xmlns:local="clr-namespace:AutoActions.Views"
             d:DataContext="{d:DesignInstance Type=info:AutoActionsInfo, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" MinWidth="360" MinHeight="300">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="/Logo.ico" Width="72" Height="72" RenderOptions.BitmapScalingMode="HighQuality" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Style="{StaticResource TitleTextBlock}" Text="{Binding ProductName}" HorizontalAlignment="Center" Margin="0,12,0,0"/>
        <TextBlock Grid.Row="2" Style="{StaticResource SecondaryTextBlock}" Text="{Binding Version, StringFormat={}{0}}" HorizontalAlignment="Center" Margin="0,2,0,0"/>

        <Border Grid.Row="3" Style="{StaticResource CardBorder}" Padding="{StaticResource CardPadding}" Margin="0,20,0,0">
            <StackPanel>
                <TextBlock Style="{StaticResource DefaultTextBlock}" Text="{x:Static pres:ProjectLocales.AboutBasedOn}" TextWrapping="Wrap" Margin="0,0,0,12"/>
                <UniformGrid Columns="2">
                    <Button Style="{StaticResource SecondaryButton}" Content="{x:Static pres:ProjectLocales.UpstreamProject}" Command="{Binding OpenUpstreamCommand}" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="{x:Static pres:ProjectLocales.ForkRepository}" Command="{Binding OpenForkCommand}" Margin="4,0,0,0"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
